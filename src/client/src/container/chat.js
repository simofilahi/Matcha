import React from "react";
import {
    ChatPage,
} from '../components';

const ChatContainer = (props) => {
    return (
        <ChatPage>
           
        </ChatPage>
    );
}

export default ChatContainer;