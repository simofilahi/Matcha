import React from "react";
import {
    ExplorePage,
} from '../components';

const ExploreContainer = (props) => {
    return (
        <ExplorePage>
           
        </ExplorePage>
    );
}

export default ExploreContainer;
