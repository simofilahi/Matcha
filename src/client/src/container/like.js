import React from "react";
import {
    LikePage,
} from '../components';

const LikeContainer = (props) => {
    return (
        <LikePage>
           
        </LikePage>
    );
}

export default LikeContainer;