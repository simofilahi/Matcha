import React from "react";
import {
    Home,
} from '../components';

const Homecontainer = (props) => {
    console.log(`props ==> ${props}`);
    return (
        <Home>
           
        </Home>
    );
}

export default Homecontainer;
