import React from "react";
import {
    Home
} from '../components';

const Homecontainer = (props) => {
    return (
        <Home>
            <Home.Header />
        </Home>
    );
}

export default Homecontainer;
