import React from "react";
import {
    ViewPage,
} from '../components';

const ViewContainer = (props) => {
    return (
        <ViewPage>
           
        </ViewPage>
    );
}

export default ViewContainer;
