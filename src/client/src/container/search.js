import React from "react";
import {
    SearchPage,
} from '../components';

const SearchContainer = (props) => {
    return (
        <SearchPage>
           
        </SearchPage>
    );
}

export default SearchContainer;
